const enum QueryKeys {
  wines = 'wines',
  wineById = 'wineById',
  promotion = 'promotion',
  token = 'token',
  isLoggedIn = 'isLoggedIn',
  user = 'user',
  isVisited = 'isVisited',
}

export default QueryKeys;
