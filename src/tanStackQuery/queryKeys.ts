const enum QueryKeys {
  wines = 'wines',
  wineById = 'wineById',
  promotion = 'promotion',
  token = 'token',
  isLoggedIn = 'isLoggedIn',
  user = 'user',
  isVisited = 'isVisited',
  favorites='favorites'
}

export default QueryKeys;
